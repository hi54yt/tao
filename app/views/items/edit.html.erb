<h1>Editing item</h1>
<%= form_for @item, :url => item_path(@item), :html => { :multipart => true } do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<% end %>

<%= link_to 'Show', @item %> |
<%= link_to 'Back', items_path %>
